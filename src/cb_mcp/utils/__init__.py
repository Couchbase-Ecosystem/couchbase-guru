"""Couchbase MCP utilities: settings store (config), constants, and the agent client.

Import directly from the submodules (``cb_mcp.utils.config``,
``cb_mcp.utils.constants``, ``cb_mcp.utils.agent``) rather than through this
package, so there is a single source of truth for each symbol.
"""
